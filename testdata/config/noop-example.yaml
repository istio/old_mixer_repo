apiVersion: "config.istio.io/v1alpha2"
kind: mixer-noop-reporter
metadata:
  name: handler
  namespace: istio-config-default
spec:
---
apiVersion: "config.istio.io/v1alpha2"
kind: rule
metadata:
  name: example
  namespace: istio-config-default
spec:
  match: "true" # If omitted match is true.
  actions:
  - handler: handler.mixer-noop-reporter
    instances:
    - accesslog.logentry
